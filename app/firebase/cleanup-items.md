# LaundryOps Cleanup Items

Use this as the running cleanup list before beta and before app-store packaging.

## Firebase / Deploy

- Complete the Sunstate separation from LaundryOps Firebase. Verify Sunstate Dashboard V2 sign-in, all dashboard pages, `syncAmazonIncomeSheet`, and the exact 45-record dataset in `sunstate-commercial-dashboard`; switch normal use to the V2 Hosting URL; then inventory and, only with separate approval, remove the legacy Sunstate Hosting, Function, and Amazon data from `laundromat-maintenance-app`. Preserve all LaundryOps resources throughout the cleanup.
- [x] Cloud Functions build-image cleanup configured. Artifact Registry repository `projects/laundromat-maintenance-app/locations/us-central1/repositories/gcf-artifacts` now automatically deletes container images older than 7 days.
- [ ] Investigate the recurring Cloud Functions build-image cleanup warning after a future deployment. Confirm the seven-day Artifact Registry policy is removing eligible `gcf-artifacts` images; review the exact remaining images and expected storage cost before deleting anything manually.
- [x] GitHub Actions now validates the Functions TypeScript build. The workflow uses Node.js 22 to run the Functions TypeScript check and build, blocks deployment if either fails, and passed in pull-request Actions run 33.
- [x] GitHub Actions requires both `VITE_BILLING_API_BASE_URL` and `VITE_FUNCTIONS_API_BASE_URL` before production deployment. On July 21, 2026, both GitHub repository secrets were reset to the verified LaundryOps Functions URL, and GitHub confirmed each update. The workflow checks both values exactly before any coordinated production deployment.
- [x] GitHub Actions includes the exact `storage:laundryopsStorage` target in the coordinated LaundryOps deployment with Hosting and Firestore rules. Pull-request Actions run 37 passed the Java 21 Firestore and Storage emulator suite for committed HEAD `87ecdcf`.
- [x] Primary tenant permanent access completed July 21, 2026. `X3UZ6Qjyzi12dfAGgB7o` preserves its original trial history with `trialEndsAt` restored to `2026-06-27T13:41:49.394Z` and now has a backend-controlled `developer` entitlement. Firestore and Storage rules, the six affected Functions, and Hosting were deployed after local and GitHub validation. The live write was guarded by fresh organization, owner, admin-membership, trial, and Stripe checks; post-write verification confirmed permanent access and no Stripe customer or subscription IDs.
- [x] The workflow targets the seven exact LaundryOps Functions and never uses a broad Functions deployment. On July 21, 2026, all seven were deployed successfully to `laundromat-maintenance-app` in `us-central1` on Node.js 22. `syncAmazonIncomeSheet` remained deployed but was excluded and was not modified. Unauthenticated checks returned `401` for all six protected application endpoints, and the unsigned Stripe webhook check returned `400`.
- [x] Server-side Firestore transaction rate limits are implemented, tested, deployed, and live-verified. On July 21, 2026, authenticated user `reollc@gmail.com` sent six checkout requests through a profile linked only to the confirmed-deleted organization `yPfQm2WaeuTFOa752NU0`. Requests 1-5 stopped with `Organization access not found.` before Stripe; request 6 was blocked with `Too many requests. Please try again shortly.` The matching `functionRateLimits` document remained at count `5`, proving the rejected request did not increment the counter. No checkout page opened and no protected tenant data was used. Stripe webhook retries remain protected by signature verification instead of user rate limiting.
- [x] Firebase rules tests and manual AI setup docs now match the current manual upload Storage path: `orgs/{orgId}/manuals/{userId}/{manualId}/{fileName}`. The client, Storage rules, backend path validator, and setup guide agree. Emulator validation passed 18/18, including role, tenant, path-shape, read, delete, and 25 MB boundary cases.
- [x] Future-project Developer Connect setup requirement documented in `firebase/developer-connect-setup.md`. For new repository connections created after September 21, 2026, explicitly enable `secretmanager.googleapis.com` first. This is not a LaundryOps live blocker, and no live API change was made.
- [x] Firebase Authentication roster reviewed read-only before launch. All 10 login identities were reconciled against current Firestore profiles, organization owners, memberships, protected organization data, and Storage paths.
- [x] Auth users classified into cleanup buckets: four real or protected accounts to keep, six clearly synthetic `example.com` accounts approved for deletion, and no unresolved inspect bucket.
- [x] Confirmed that deleting a Firebase Auth user does not automatically delete that user's Firestore profile, organization, machines, work orders, manuals, Storage files, sessions, or billing-linked data.
- [x] Ownership and membership safeguards completed for the current roster. None of the six synthetic candidates owned or belonged to either protected Lake Eustis organization, and none appeared in the 2,560 protected organization documents scanned.
- [x] Deleted the six specifically authorized synthetic Firebase Auth accounts and five matching `users/{uid}` profile documents after a fresh overlap and billing check. Stripe and the two protected Lake Eustis organizations were not changed.
- [x] Verified `laundrytracker2024@gmail.com` remains intact in Firebase Authentication. The correct UID is `d7SFxEYdMPUGOFjncb0GQXrxQxI3` (zero after `jncb`, not the letter O). Its `users/{uid}` profile still points to organization `X3UZ6Qjyzi12dfAGgB7o`, and its active `admin` membership remains in place. No Auth restoration or data relinking was required.
- [x] Tenant consolidation completed July 21, 2026. `organizations/X3UZ6Qjyzi12dfAGgB7o` remains the primary Lake Eustis tenant with its indexed manuals, memberships, machines, maintenance records, and Storage files preserved. At Robert's explicit direction, `organizations/HptNReMSNSocuSOCCuRr` and its 61 nested Firestore documents were deleted recursively after a final preflight; its Storage prefix was empty, and the shared owner Auth account and user profile were preserved. Robert will manually add machines `37` and `E`, correct duplicate machine `32`, and recreate or review the W12 maintenance record as needed in the primary tenant.
- [ ] Deploy and verify the committed Account-page privacy/current-trial UI. The hosted app still displayed the old technical `User Session` panel and a fresh 14-day trial fallback for `reollc@gmail.com`, whose profile points to the deleted organization `yPfQm2WaeuTFOa752NU0`. Confirm the current build hides technical session details and handles a missing/deleted organization without presenting an active trial.

## Repair Assist / Manuals

- Smoke test real PDF manual upload from the live app.
- Smoke test `indexOrganizationManual` with a real manufacturer manual.
- Smoke test `generateRepairAssist` with a known manual error code and confirm citations come from the uploaded manual.
- Add UI visibility for `indexingStatus` so an indexed manual can show when a replacement index is processing or failed.
- Confirm OCR handling for scanned/image-based PDFs; current extraction depends on readable PDF text.
- OCR support will be added before full release.
- Keep beta manual Storage uploads role-gated. Current rule allows only owner/admin/manager users to upload a PDF under their own UID folder.
- Revisit Storage PDF validation after beta. Current beta rule may allow `.pdf` filename extension in addition to content type; true validation happens when the backend parser indexes the uploaded file.

## Beta Readiness

- Run a full beta pass: create account, add machine, upload manual, create work order, run Repair Assist, start checkout, confirm Stripe/Firebase state.
- Re-check live authorized domains and Firebase web config after each hosting/domain change.
- Show and enforce trial dates clearly. Current Lake Eustis beta org is marked trialing from May 28, 2026, which means a strict 14-day trial would have ended June 11, 2026.
- Build the full trial-expiration gate. Firebase login only proves the user's identity; the app must also check whether the user's company account is allowed to use LaundryOps. On company setup, save `trialStartedAt`, `trialEndsAt`, and `subscriptionStatus: "trialing"` on `organizations/{organizationId}`. Every time the app opens, and while the user is using it, check that company record. If today is past `trialEndsAt` and there is no active Stripe subscription, show a lock screen with only `Choose Monthly`, `Choose Annual`, `Manage Billing`, and `Sign Out`. Also enforce the lock in Firestore rules, Storage rules, and Cloud Functions so expired accounts cannot add machines, upload manuals, create work orders, or use AI Repair Assist even if they stay logged in.
- Add an in-app timer/check that locks the screen as soon as the 14-day trial passes, without requiring the user to sign out or refresh.
- Review Google Play beta packaging requirements once the web workflow is stable.
